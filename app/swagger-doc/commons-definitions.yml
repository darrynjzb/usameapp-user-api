parameters:
  errorIncorrectBody:
    type: "object"
    properties:
      code:
        description: "Code of error"
        enum:
        - CREATE_USER_ERROR_BODY
        type: "string"
      message:
        description: "Description of error"
        type: "string"

  errorConflicError:
    type: "object"
    properties:
      code:
        description: "Code of error"
        enum:
        - DATABASE_CONSTRAIN_ERROR
        type: "string"
      message:
        description: "Description of error"
        type: "string"

  errorInternal:
    type: "object"
    properties:
      code:
        description: "Code of errors"
        enum:
        - INTERNAL_SERVER_ERROR
        type: "string"
      message:
        description: "Description of error"
        type: "string"
